import 'package:flutter/material.dart';

class AppColors {
  // ── Fondos ──────────────────────────────────────────────────
  static const Color background   = Color(0xFFF7F7F7); // gris suave (scaffold)
  static const Color surface      = Color(0xFFFFFFFF); // blanco puro (cards, nav)
  static const Color card         = Color(0xFFFFFFFF);

  // ── Texto ───────────────────────────────────────────────────
  static const Color textPrimary   = Color(0xFF161823); // negro TikTok
  static const Color textSecondary = Color(0xFF888888); // gris medio

  // ── Marca ───────────────────────────────────────────────────
  static const Color primary      = Color(0xFFFE2C55); // rojo TikTok
  static const Color primaryLight = Color(0xFFFFF0F3); // rojo muy suave (fondos)
  static const Color secondary    = Color(0xFF25F4EE); // cyan TikTok
  static const Color error        = Color(0xFFFF6B35); // naranja error

  // ── Semánticos ──────────────────────────────────────────────
  static const Color warning      = Color(0xFFF0A830); // amarillo consejo
  static const Color success      = Color(0xFF22C55E); // verde éxito

  // ── Bordes ──────────────────────────────────────────────────
  static const Color border       = Color(0xFFEBEBEB); // borde suave universal
}